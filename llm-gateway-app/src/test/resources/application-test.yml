spring:
  main:
    web-application-type: servlet
  flyway:
    enabled: false

management:
  server:
    port: 0

gateway:
  accounting:
    # This profile uses the in-process journal fixture, not financial database writes.
    completion-window: 0s
  security:
    enabled: false
  rate-limit:
    enabled: false
  guardrails:
    enabled: false
  resilience:
    circuit-breaker:
      enabled: false
  providers:
    openai:
      api-key: ""
    openrouter:
      api-key: ""
    bedrock:
      enabled: false
